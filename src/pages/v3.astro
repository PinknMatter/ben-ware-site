---
// V3 — "The Frontispiece" — was chosen, and is now the site. This address is
// in the client's email thread, so it redirects rather than 404s.
import Redirect from '../components/Redirect.astro';
---

<Redirect to="/" />
